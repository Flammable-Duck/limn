---
Title: Testing
# Date: Thu Aug 15 2024
Date: 3
---

This is a test!
**Formatting** is supported and stuff which is *super* cool <br>
i even got :sparkle: emotes :sparkle: to work:bang: :grin:

:new: :snicker: :grin: :headphones: :bang: :mp3: :ship: :duck: :sparkle:

```go
func main() {
    page := map[string]pages.Page{
        "/": pages.NewIndex("markdown/"),
        "/notes": pages.NewNotes("markdown/notes/"),
    }

    for k, c := range page {
        log.Printf("adding handler at %s", k)
        http.HandleFunc(k, c.Handler)
    }
//                                                                           80|
    fs := http.FileServer(http.Dir("./assets"))
    http.Handle("/assets/", http.StripPrefix("/assets/", fs))

    log.Fatal(http.ListenAndServe(":8080", nil))
}
```
